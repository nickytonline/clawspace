# Optional: override workspace root (defaults to parent of workspace-astro)
CLAWSPACE_ROOT=/absolute/path/to/workspace

# Optional: extra comma-separated ignore patterns
CLAWSPACE_IGNORE=".pnpm,dist,logs"

